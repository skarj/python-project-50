import json


def format_json(diff):

    new_json = {}

    def format(data):
        for k, v in sorted(data.items()):
            return

    format(diff)

    print(json.dumps(new_json, indent=4))

    return []
